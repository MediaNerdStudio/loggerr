services:
  loggerr:
    build: .
    container_name: loggerr
    ports:
      - "3000:3000"
      - "9100-9199:9100-9199"
    environment:
      PORT: 3000
      DATA_DIR: /app/data
      MEDIA_DIR: /app/media
      TZ: Europe/Amsterdam
    volumes:
      - ./data:/app/data
      - ./media:/app/media
    restart: unless-stopped
